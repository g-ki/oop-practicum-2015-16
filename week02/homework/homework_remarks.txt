Дарко - Излишен & пред Employee във функцията addEmployee, липсва & пред Firm в removeEmployee и в changeSalary, грешна формула за пресмятане на увеличението на заплатата с процент, излишен if-else във viewEmplpyees (възможно е да и само с един if).

Дилян - Замяна на служител в removeEmployee с друг вместо премахването му, грешна формула за пресмятане на увеличението на заплатата с процент.

Калоян - Използване на глобална фирма, по този начин функциите ти работят само за нея. Грешна формула за пресмятане на увеличението на заплатата с процент.

Кристин - Излишно използване на флагове във функциите isEmployed и isFullFirm.

Мариела - Липса на повечето функции.

Мариета - Използване на глобален масив от служители, излишен & пред n в няколко функции, неколкократно задаване на заплата в addEmployee, различна сигнатура на removeEmployee - трябваше да се подаде име, а не индекс в масива, липсва функцията changeSalary.

Росица - Излишен аргумент Employee& m в няколко функции, използване на глобален масив от служители, промяна на сигнатурата на няколко фунцкии - подаване на индекс в масива вместо низ.

Христо - Използване на указател към служител като аргумент на функциите вместо фирма, излишни флагове в isEmployed, подаване на служител вместо низ в isEmployed, removeEmployee.